# SEO Coach 2.0 — 大版本更新介紹

> 從「回答你 SEO 問題的工具」，變成「陪你在自己網站上練到會做 SEO 的教練」。

上一個公開版本是 **1.1.0**。如果你用的是那一版，這次升級會一次拿到 1.2、2.0 與 2.1 的全部改動——這也是為什麼它是大版本，而不是一次功能追加。

---

## 一句話說明差在哪

**1.1.0**：你問，它答；跑完一輪檢查，你拿到一份發現清單。

**2.0**：它先問你要什麼結果，帶你建立修改前的基準，示範一次怎麼看，陪你做第二次，第三次放手讓你做；你動手改一項，它要求重驗，並在正確的時間回來看數據；最後換一個你沒看過的頁面，測你能不能自己重做一次。

差別不在內容多寡，在於**它不再把「你看完了」當成「你會了」**。

---

## 三個最大的改變

### 1. 主線從「18 個模組」換成「一個真站專案」

以前的主線是把 18 個模組跑完。現在的主線是 **G0–G7 學徒制**，全部在你自己的網站上進行：

```text
G0  目標與安全
G1  修改前基準          ← 沒有基準，不准動任何東西
G2  確認 Google 看得到
G3  搜尋機會實驗室       ← 候選查詢 → SERP 意圖 → 內容缺口 → 承接頁 → brief
G4  一項低風險、可還原的修改
G5  修改後技術重驗
G6  在預定的窗口回看，決定保留／回復／再測
G7  換陌生頁面的畢業考 + 7 天後遷移複測
```

18 個模組還在，但降級成**教練按需調用的知識庫**，不再是要你照順序上完的課表。「流量掉了」不會再自動展開整套課程。

### 2. 進度從「上過幾課」換成「能力證據」

你的紀錄不再是打勾清單，而是五種能力狀態：

| 狀態 | 意思 |
|---|---|
| `unseen` | 還沒接觸 |
| `demonstrated` | 看過教練示範，能說出判斷依據 |
| `guided` | 在提示下完成過同型任務 |
| `independent` | 無提示獨立完成 |
| `retained` | 隔一段時間、換一個案例仍做得到 |

同時記錄**提示級別、犯過的錯、換案例的表現、1／7／30 天複測日期**。

一次答對不會標記為「會了」。延遲複測後換個案例還做得到，才算真的留在你身上。

### 3. 教學法反轉：新手不再被逼著猜

1.2.0 的做法是先丟一個二選一預測題（「你猜 Google 找得到你的網站嗎？」）。實測下來，這對完全零基礎的人只是換一種形式的考試。

**2.0 改成先示範一個真實結果，再把下一個同型判斷交給你。** 你說「我不懂」或「你先看」時，教練會立刻示範，不會繼續用問題考你，也不會走提示階梯。

---

## 三條學習路線

### 從零到第一個可驗證成果（G0–G7）

適合不知道從哪開始、或學了很多卻沒完整做過一次的人。完成後你手上不是一份報告，而是一條**你能再次執行的工作方法**。

### 搜尋機會實驗室

適合學選題、關鍵字、搜尋意圖、內容缺口、頁面歸屬與 content brief。

**沒有 Ahrefs／Semrush 也能完成基本訓練**：開新的無痕視窗，記錄查詢、地區、語言、裝置與日期；教練示範第 1 個自然結果，下一輪共做第 2 個，第 3 個你自己判讀。只有三筆意圖仍混合時才再多看兩筆——不會一開始就要你分析 Top 10。

而且它會擋掉兩個最常見的誤判：**競品有寫不代表你也該寫，競品沒寫也不自動等於機會**。每個內容缺口都要再核對四件事：搜尋端有沒有需求、你的站是不是已經承接了、你的網站能不能可信地提供、對你的生意有沒有價值。缺一項就只能留作候選，不能升級成確認的機會。

### 四篇 SEO 寫作學徒制

不是叫 AI 幫你寫四篇文章，是用四個難度遞增的作品把研究與寫作能力交還給你：

- **A1** 教練示範與共作
- **A2** 同型任務，你做更多判斷
- **A3** 換搜尋意圖或頁型，測能不能遷移
- **A4** 無提示 Capstone

每篇都要留下 SERP 證據、brief、原稿、修改稿、回饋、QA、發布與觀察紀錄。**畢業看八項能力證據，不看 AI 給的總分**——外掛的綠燈和分數只能當線索。

---

## 同樣的問題，這版的回答會不一樣

如果你用過 1.x，這些行為變了：

| 情境 | 舊版 | 2.0 |
|---|---|---|
| 完全新手第一輪 | 先給你一題猜 | 先示範一個結果 |
| 你說「你先看」 | 走提示階梯 | 直接示範 |
| 功課沒做完 | 擋住下一步 | 只有真正的證據依賴或安全風險才擋，其餘縮小成 5–10 分鐘版或先擱置 |
| 檢查沒發現問題 | 再找一個，直到挖出東西 | **健康基線也是成果**，不製造假陽性 |
| `site:` 結果 | 當成收錄數 | 只是粗略快篩，單一 URL 用 GSC 網址審查定案 |
| 中文 title 長度 | 鎖 25–30 字 | 沒有通用固定字數門檻 |
| 「多久會有效？」 | 3–6 個月 | 不用通用時間表判定你的網站 |
| KD 數值 | 當門檻用 | 標成第三方估值，不是 Google 的分數 |

**升級提醒**：`seo-progress.md` 的 schema 變了（改記能力狀態而非模組打勾），並新增第三個檔案 `seo-ga4-log.md`。如果你有工具在讀這些檔，要留意。

---

## 看懂成效，而不是追著排名跑

新增 **GA4 陪跑軌道**：一輪只給**一張指路卡**——精確點擊路徑（同時給中文字樣和英文原名，因為 GA4 各版本選單不一致）→ 只看一個數字 → 講掉一條常見誤讀 → 換你做。L1–L6 每級只解鎖一個概念。

**數字一出現，教練會主動做三件事**（不等你問）：用人話翻譯這個數字在算什麼、跟台帳上次比或明講這是基準線、當輪寫進台帳並告訴你。

新增 `seo-ga4-log.md`：只增不刪的數據台帳，每一列都要填「當時發生什麼」——三個月後看到波動，那一欄是唯一能把因果對起來的東西。台帳只寫你這輪真的看到的數字，來源必填「截圖／API／口述」。

想接 GA4 Data API 也有逐步教學，但**接通的定義是 API 數字跟介面數字對得起來**，不是腳本不報錯；而且接了 API，教練仍會要求你自己在介面看一次同一個數字。

---

## 零預算也走得下去：免費工具地圖

工具清單依官方頁面於 **2026-08-01** 核對，包含關鍵字擴展、難度、SERP、排名查詢、流量估算、外鏈、壞連結、網域權重，以及三個 AI 可見度工具（AI Visibility Checker、AI Overviews Tracker、AI Mode Tracker）。

但工具**不會在第一輪丟給你**。它按進度分層開放：

- **前期（學讀畫面）**：不提工具，先讓你用眼睛看懂一個搜尋結果。
- **中期（開始排序取捨）**：工具當**驗證層**——你先講你的判斷，再打開工具對答案。不一致的地方就是最好的教學素材。
- **回看階段**：才用排名查詢工具搭配 GSC 平均排名。

理由很簡單：**看不懂 SERP 的人拿到 `KD = 23`，也不知道那代表什麼。** 順序反過來，你會學會查數字，但不會學會判斷。

所有工具數字（KD、DR、流量估算、AI 提及次數）一律標成第三方估值，不會冒充 Google 或 AI 平台的內部資料。

---

## 回來就能接著學（選用）

新增 `hooks/`：一支 router 同時支援 Claude Code 與 Codex。

- 在**陪跑資料夾**（有進度檔的那個）開新 session 會自動接續上次進度。
- 在任何專案，偵測到新手向的 SEO 提問才會觸發載入。
- 注入的文字會要求「若更適合其他更專門的 SEO 技能，就改用那一個」，不會把整站 audit、語意內容或 GEO 硬吃進陪跑模式。

**不裝完全不影響既有行為。** 無第三方套件、不寫檔、不連網，任何例外靜默結束。

---

## 不是靠感覺：可驗證的教練行為

2.0 第一次把「教得好不好」納入可執行驗證，而不只檢查檔案格式能不能讀：

- **86 個 eval case、260 條判準**，每個 case 送進獨立的全新 session 與空白工作目錄，記錄回應與實際產生的檔案。
- 涵蓋新手帶法、延遲複測、進度更新、常見誤判、寫作驗收與搜尋機會實驗室。
- 用隱私反例確認客戶資料、私人研究、未公開 prompt 與其他本機專案不會進入公開教材。
- **判準補完**：改版前 146 條 assertion 裡有 81 條（55%）在評分表找不到定義；現在 100% 都有可引用的判準，而且驗證器加了閘門——assertion 沒有對應判準就直接 build 失敗。

---

## 公開來源與隱私邊界

所有教材限於：Google 等官方公開文件、公開課程、公開網站與 SERP、學員授權的自有資料，以及 AK 在 Threads 上**有公開 URL** 的理論與觀點。

**不包含**私人研究方法、未公開測試、客戶資料、客戶案例細節、私訊、未公開 prompt、私人 SOP，或其他本機專案。

這條邊界由三層機制守住：來源政策文件、失效模式對照表，以及每次建置前執行的隱私外洩反例測試。個人品牌來源另有 URL 白名單與檔案雜湊登錄，內容被動過就會建置失敗。

這不是缺少內容，是刻意保護客戶、研究資產與公開教學之間的界線。SEO Coach 給你的是一套扎實的公開基礎與學習方法，不是 AK 私人方法論的複製品。

---

## 誠實說明它做不到什麼

- 不是一鍵全站 audit 或健康分數產生器。
- 不保證排名、流量或索引結果。
- 不代寫完整文章、不代交策略、不做大量自動發布。
- 不會用固定 KD、字數、內鏈數或外掛分數替你下結論。
- 沒有證據時不會把猜測說成已確認。

大型遷站、重大流量崩跌、大量索引異常、企業級國際 SEO、程式化建頁，以及醫療、金融、法律等高風險判斷，仍可能需要工程師或合格專業人員介入。

還有一件該說清楚的：**它的教學方法比多數入門課程嚴謹，但目前還沒有大量學員完整走完 G0–G7 的成果資料可以拿出來。** 它賣的不是捷徑，是一個少走彎路的開始。

---

## 升級方式

沿用原本的安裝路徑覆蓋即可。把下面整段複製給你的 AI Agent：

```text
請幫我更新這個 SEO Coach Skill 到最新版，覆蓋原有的 seo-coach 目錄；先不要啟用可選的 hooks。完成後告訴我實際安裝路徑。

https://github.com/akseolabs-seo/seo-coach
```

升級後有兩件事要留意：

1. `seo-progress.md` 的欄位結構變了，教練會在下次 session 引導你補齊，不會覆蓋你已有的紀錄。
2. 第一次拿到 GA4 數字時會新建 `seo-ga4-log.md`。

---

*詳細逐版變更見 [CHANGELOG.md](CHANGELOG.md)。*
